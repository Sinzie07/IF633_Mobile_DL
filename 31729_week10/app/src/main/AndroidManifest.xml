<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="umn.ac.a31729_week10">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.31729_week10">
        <activity android:name=".SimpleIntentService"></activity>
        <activity android:name=".CustomServiceTask" />
        <activity android:name=".CustomService" />
        <activity android:name=".CustomBoundService" />
        <activity android:name=".week10c_31729" />
        <activity android:name=".week10b_31729" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <service android:name=".SimpleIntentService" />
    </application>

</manifest>